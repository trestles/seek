
here are <strong>USERS (<%=@users.count %>):</strong>
<% if signed_in? %>
  <% @users.each do |user| %>
     <%=user.ig_name %>
  <% end %>
<% else %>
  </br> Please, sign in to see the list.
  <%= button_to 'Sign In with Instagram', 'auth/instagram' %>
<% end %>
